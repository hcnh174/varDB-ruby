<#import "/global.ftl" as vardb>

<@vardb.fullscreenlayout title="Blast results" location={'Blast':'/blast/blast.html'} cur="${results.blastType?lower_case}">

<#if image??>
<@vardb.blastFigure image/>
</#if>

<div id="resultsdiv"></div>

<@vardb.addScript>
var counts=${countsjson.json};
</@vardb.addScript>

<@vardb.onReady>
	
	var results=new Ext.ux.vardb.blast.Results(
	{
		renderTo: 'resultsdiv',
		list_id: '${results.id}',
		blasttype: '${results.blastType}',
		counts: counts
	});
</@vardb.onReady>

</@vardb.fullscreenlayout>
